DATABASE_URL=postgres://fluesterlos:fluesterlos@db:5432/fluesterlos
# One-time secret for first owner registration (Host UI). Change before first boot.
BOOTSTRAP_TOKEN=change-me-once
PORT=3000
# Used for join URLs and cookie Secure flag (https \u2192 Secure cookies).
PUBLIC_URL=http://localhost:8080
VITE_API_URL=
# Persistent uploads: voucher PDFs, payslips, host QR (mounted in compose).
DATA_DIR=/data
# SMTP for voucher email after host confirms payment (optional until needed).
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
